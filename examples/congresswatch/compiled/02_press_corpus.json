{
  "id": "press_corpus",
  "type": "input_data",
  "name": "Press releases (slice window)",
  "source": {
    "doc": "examples/congresswatch/methodology_raw.md",
    "section": "§2",
    "lines": [
      26,
      35
    ]
  },
  "inputs": [],
  "output_schema": {
    "columns": [
      {
        "name": "doc_id",
        "type": "str",
        "nullable": false
      },
      {
        "name": "entity_id",
        "type": "str",
        "nullable": false,
        "description": "M:<bioguide_id> — links to members_universe."
      },
      {
        "name": "source_class",
        "type": "str",
        "nullable": false,
        "range": [
          "press_release",
          "lobbying_filing"
        ]
      },
      {
        "name": "title",
        "type": "str",
        "nullable": true
      },
      {
        "name": "body",
        "type": "str",
        "nullable": false
      },
      {
        "name": "published_at",
        "type": "date",
        "nullable": false
      },
      {
        "name": "url",
        "type": "str",
        "nullable": true
      }
    ],
    "estimated_rows": 500,
    "primary_key": [
      "doc_id"
    ]
  },
  "connector": {
    "kind": "file",
    "params": {
      "path": "examples/congresswatch/data/press_corpus.csv",
      "format": "csv"
    },
    "refresh": "per_slice",
    "notes": "Built by build_slice.py from ~/Downloads/data/data/congress_press/<month>.jsonl.\nFilter applied at slice time: text or title matches healthcare topic regex.\n"
  },
  "compiler_notes": [
    "Only press_release source_class is populated in v1. Lobbying filings flow through a separate input stage (lobbying_filings) because their entity attribution is different (filer, not member)."
  ]
}