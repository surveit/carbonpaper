[pytest]
testpaths = tests app evals
pythonpath = . tests
addopts = -q -m "not live_llm"
markers =
    live_llm: end-to-end smoke against the real LLM backend — spends tokens; opt in with -m live_llm
