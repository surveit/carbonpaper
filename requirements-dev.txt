# Dev/test-only dependencies (not needed to run the app).
-r requirements.txt
pytest>=8.0
# Lint + typecheck are pinned exactly so new rule releases can't break CI;
# bump deliberately.
ruff==0.15.20
mypy==2.1.0
