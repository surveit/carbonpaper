__pycache__/
*.pyc
*.pyo

# Local server / probe logs
*.log

# OS
.DS_Store
Thumbs.db
build/

# Agent-tool worktree scratch dirs (ephemeral, nested git repos)
.claude/worktrees/

# Persisted compilation objects (file-backed, like examples/<m>/runs)
compilations/

# Chat session transcripts (file-backed, per app/chat/store.py)
app/agent/_sessions/

# Example methodology content + scratch pipelines. Generated run outputs land
# here, and lint/typecheck skip these dirs (ruff respects .gitignore).
# Already-tracked files under examples/ stay tracked; new files need `git add -f`.
examples/
saydo/
