"""Committed example projects, portable via app.services.project.

data/<name>.json is a WorkflowFile fixture; a sibling data/<name>.csv, when
present, is sample input data (WorkflowFile carries none). Regenerate via the
capture scripts (e.g. capture_lobbying.py); never hand-edit files under data/."""
