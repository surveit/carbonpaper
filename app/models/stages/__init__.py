"""`stage_base.py` holds the type vocabulary and `StageBase`; each per-type module
holds one family of stage types' config block, its `StageBase` subclass, and the
column checks that subclass runs. `app.models.stage` unions those into `Stage`."""
