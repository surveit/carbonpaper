"""Top-level interface layer: the hydrated agents, one subpackage per agent
(compiler = the workflow-authoring/editing agent; tutorial = the scripted product
tour). Each registers on the shared app.core.agent engine; app.main mounts them."""
