"""Top-level interface layer: the hydrated agents, one subpackage per agent
(compiler = the workflow-authoring/editing agent). Each registers on the shared
app.core.agent engine; app.main mounts them."""
