"""The tutorial agent: a scripted first-run tour of carbonpaper.

It seeds a committed sample workflow, runs it, and walks the reader through the run
it just produced. It holds no editing tools — authoring is what the reader does next."""
