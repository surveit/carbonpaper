"""The per-project editing agent that authors a workflow interactively.

The generic chat spine is reused verbatim: streaming in `app.web.chat_router`; turns,
session store, and the SDK engine in `app.core.agent`."""
