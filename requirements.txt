fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
pandas>=2.0
pyarrow>=15.0
python-multipart>=0.0.9
claude-agent-sdk>=0.2.10   # real LLM backend for llm_transform stages (needs the claude CLI installed)
mcp>=1.20                  # streamable-HTTP MCP server (app/mcp) — already a claude-agent-sdk dependency
pydantic>=2                # DAG contract models (app/models.py)
